# Files

- [Database Migrations](database-migrations.md) - How drizzle-kit generates ordered PostgreSQL migrations from lib/db/schema.ts, how they are applied locally via npm scripts, and how scripts/migrate-production.ts gates auto-application behind VERCEL_ENV=production at deploy time.
- [Environment Variables & Secrets](environment-variables.md) - Catalog of every environment variable the application reads, who reads it, whether it is required or has a default, and whether the Next.js build pipeline emits it to the client (NEXT_PUBLIC_*). Documents the env-var-overlaid-by-DB-key resolution model that backs per-user API key overrides, the redaction contract enforced on every log line that could carry a secret value, and the validateEnvironmentVariables gate that runs before sandbox creation.
- [Security Policy & Log Redaction Rules](security-and-redaction.md) - The two-layer defense that keeps secrets out of user-visible logs - the static-string rule enforced in code review and the redactSensitiveInfo regex pass that scrubs known credential patterns from any sandbox output that reaches the UI - plus the env-var never-expose list and the client-safe (NEXT_PUBLIC_) boundary.
